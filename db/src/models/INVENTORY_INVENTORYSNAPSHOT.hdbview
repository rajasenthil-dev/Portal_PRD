VIEW "INVENTORY_INVENTORYSNAPSHOT"
AS SELECT SKU, PORDUCT_CODE, PRODUCT_DESCRIPTION, SIZE, LOT, EXPIRY_DATE, WAREHOUSE_STATUS, REPORT_DATE, DAYS_UNTIL_EXPIRY, ON_HAND, UPC 
FROM "SYNINVENTORYSNAPSHOTDOC"